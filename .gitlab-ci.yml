image: docker:1.13

services:
  - docker:dind

stages:
  - test

variables:
  DOCKER_DRIVER: overlay
  SUDO_ASKPASS: /usr/bin/ssh-askpass

test:
  tags:
    - dind

  script:
    - apk add --update py2-pip
    - pip install docker-compose
    - docker-compose run master bash -c "yes | salt-key -A && salt '*' state.sls template"
